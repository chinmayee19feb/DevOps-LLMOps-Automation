server {
    listen 80;
    
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Simple proxy - pass everything under /api to respective services
    location /api/auth/ {
        proxy_pass http://auth:3001/;
    }
}
